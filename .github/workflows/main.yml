name: Nightly Web Scraper

on:
  schedule:
    # Runs every night at midnight UTC (adjust timing as needed)
    - cron: '0 0 * * *'
  # Allows you to manually trigger the scraper from the GitHub UI for testing
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Run Scraper
        run: npm run all

      - name: Commit and push updated JSON
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add events.json
          git commit -m "chore: nightly scrape update [skip ci]" || exit 0
          git push
